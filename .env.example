# 复制本文件为 .env（与本文件同级，即项目根目录），保存后必须重启 Metro：npx expo start
# app.config.js 会 dotenv 加载本文件；未设置时使用 DEFAULT_API（与 services/config.ts 的 fallback 一致）
# 演示默认指向公网示例后端；自建请改为 http://你的IP或域名:端口（勿尾斜杠，端口与后端 uvicorn 一致，常见 3000）
EXPO_PUBLIC_API_BASE_URL=http://8.162.9.49:3000
